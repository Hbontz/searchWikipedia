# searchWikipedia

The Wikipedia Viewer uses the Wikipedia API to return Wikipedia articles based on an user’s query.  Also, the user can click a button to see a random Wikipedia entry.  Skills: JavaScript, HTML5, CSS3, Bootstrap, AJAX, jQuery, JSON
